{
  "name": "cidade-idoso-frontend-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end (Playwright) do Portal Cidade do Idoso",
  "scripts": {
    "test:e2e": "playwright test",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
